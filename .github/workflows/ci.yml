name: ci

on:
  push:
  pull_request:
  schedule:
    - cron: "0 12 * * 5" # Friday 12:00 UTC, after the weekly vote
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: sudo apt-get update && sudo apt-get install -y --no-install-recommends shellcheck
      - run: for f in sh/*.sh test/fake-gcloud test/run.sh; do sh -n "$f"; done
      - run: shellcheck -s sh -x sh/*.sh test/fake-gcloud test/run.sh
      - uses: fsfe/reuse-action@676e2d560c9a403aa252096d99fcab3e1132b0f5 # v6.0.0

  golden:
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: foundry-rs/foundry-toolchain@908c540300062bd5a7e473851cdb4282204cee09 # v1.9.1
        with:
          version: v1.8.3
      - run: TEST_SH=dash test/run.sh

  # Weekly drift check against the key project. Reads only; no stored secret.
  check:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.CI_SERVICE_ACCOUNT }}
      - uses: foundry-rs/foundry-toolchain@908c540300062bd5a7e473851cdb4282204cee09 # v1.9.1
        with:
          version: v1.8.3
      - run: sudo apt-get update && sudo apt-get install -y --no-install-recommends jq openssl
      - env:
          KEY_PROJECT: ${{ vars.KEY_PROJECT }}
          KEEPER_PROJECT: ${{ vars.KEEPER_PROJECT }}
          LOCATION: ${{ vars.LOCATION }}
          KEY_RING: ${{ vars.KEY_RING }}
          KEY: ${{ vars.KEY }}
          ADMIN_GROUP: ${{ vars.ADMIN_GROUP }}
          KEEPER_SA: ${{ vars.KEEPER_SA }}
        run: |
          cat >config.env <<CFG
          KEY_PROJECT='$KEY_PROJECT'
          KEEPER_PROJECT='$KEEPER_PROJECT'
          LOCATION='$LOCATION'
          KEY_RING='$KEY_RING'
          KEY='$KEY'
          ADMIN_GROUP='$ADMIN_GROUP'
          KEEPER_SA='$KEEPER_SA'
          CFG
      - run: sh/check.sh
